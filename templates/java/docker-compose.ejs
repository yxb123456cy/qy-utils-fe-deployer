version: "3.9"
services:
<%= appName %>:
  build: .
  ports:
  - "<%= port %>:<%= port %>"
      environment:
      - SPRING_PROFILES_ACTIVE=<%= profile %>
        restart: unless-stopped
        networks:
        - app-network

        networks:
        app-network:
        driver: bridge